name: Prepare and Tag Release

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: Semantic version tag; leave empty to use BasicsVersion
        required: false
        type: string

permissions:
  contents: write

concurrency:
  group: basics-resolver-catalog
  cancel-in-progress: false

jobs:
  prepare:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0
          fetch-tags: true
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          check-latest: true
      - id: release
        name: Resolve release tag
        shell: bash
        run: |
          set -euo pipefail
          version="$(sed -n 's/^const BasicsVersion = "\(v[0-9.]*\)"$/\1/p' model/model.go)"
          tag="${{ inputs.release_tag }}"
          [[ -n "$tag" ]] || tag="$version"
          [[ "$tag" == "$version" ]]
          [[ "$tag" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]
          git fetch --force --tags origin
          ! git rev-parse --verify --quiet "refs/tags/$tag"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
      - name: Refresh validated encrypted DNS endpoints
        shell: bash
        run: |
          set -euo pipefail
          go run ./cmd/update-resolver-endpoints \
            -input network/resolver/endpoints_sources.json \
            -output network/resolver/endpoints_embed.json \
            -timeout 4s \
            -concurrency 6
      - name: Validate release source
        shell: bash
        run: |
          set -euo pipefail
          go mod verify
          go test -race -count=1 ./...
          go vet ./...
          git diff --check
      - name: Commit refreshed catalog
        shell: bash
        run: |
          set -euo pipefail
          if [[ -z "$(git status --porcelain -- network/resolver/endpoints_embed.json)" ]]; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add network/resolver/endpoints_embed.json
          git commit -m "chore: refresh encrypted DNS endpoint catalog"
          git push origin HEAD:main
      - name: Create immutable release tag
        env:
          RELEASE_TAG: ${{ steps.release.outputs.tag }}
        shell: bash
        run: |
          set -euo pipefail
          git tag -a "$RELEASE_TAG" -m "Release $RELEASE_TAG"
          git push origin "refs/tags/$RELEASE_TAG"
